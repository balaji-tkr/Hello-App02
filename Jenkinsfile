@Library('jenkins-SL')_
import com.abc.demo

pipeline {
        agent any
        stages {
         stage('Build') {  
            steps {
             sh  'mvn clean install'
          
            }
        }
}
}
